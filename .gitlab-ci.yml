cache:
  paths:
    - .m2

stages:
  - deploy

deploy:
  stage: deploy
  only:
    - master
  script:
    - mvn clean package install -Dmaven.repo.local=.m2/repository
    - ls -R
  artifacts:
    name: "BungeeUtilisals"
    expire_in: 1 week
    paths:
      - target/*.jar